/**
 * ค่าหน้าตาที่หน้าเดลิเวอรี่ใช้ร่วมกันหลายจอ — หน้าร้าน (delivery/index) กับ
 * หน้าหมวด (delivery/[cat]) ต้องใช้เฉดและสีตัวหนังสือชุดเดียวกันเป๊ะ ไม่งั้นกดจากหน้า
 * หนึ่งไปอีกหน้าแล้วสีเพี้ยนกันให้เห็น จึงประกาศไว้ที่เดียว
 *
 * ห้ามย้ายไปไว้ในไฟล์ route เพราะ expo-router จะอ่านไฟล์ใน app/ เป็นหน้าจอ การ export
 * ค่าอื่นออกไปจากไฟล์เหล่านั้นเป็นเรื่องที่ควรเลี่ยง
 */

/* เฉดจากแบนเนอร์ภาพที่ 2 ที่เจ้าของเลือก (3 ก.ย. 2026) ดูดสีจากไฟล์จริงบน production
   ไล่แนวตั้งตามต้นฉบับ ปลายล่างเป็นครีมอ่อนซึ่งถูกดันไปอยู่ใต้ขอบหัวจอ */
export const DELIVERY_RAMP = ['#FC5738', '#FD7A50', '#FCDEB4'] as const;

/** ตัวหนังสือบนหัวจอเดลิเวอรี่ — ขาว คู่กับเงาจาง ๆ เพราะพื้นส้มสว่างเกินกว่าจะพอดี */
export const DELIVERY_INK = '#FFFFFF';

/** เงาใต้ตัวอักษรบนหัวจอ ตรึงขอบให้อ่านออกบนพื้นส้มสว่าง */
export const DELIVERY_INK_SHADOW = {
  textShadowColor: 'rgba(120,40,16,0.30)',
  textShadowOffset: { width: 0, height: 1 },
  textShadowRadius: 4,
} as const;

/** สีพื้นแผ่นเนื้อหา — วงกลมหมวดหมู่ใช้สีนี้เพื่อให้กลืนไปกับพื้น */
export const DELIVERY_SHEET_BG = '#F4F1EF';
